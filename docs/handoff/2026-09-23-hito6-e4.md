# Traspaso 2026-09-23 (tarde) — Hito 6: E4a mezclada, E4b en PR

Para continuar desde cualquier equipo. Leer antes `dotnet/AGENTS.md` (párrafos E1a…E4b), `docs/STATUS.md`, `docs/plans/2026-09-23-e4-oferta-configurable.md` (con las secciones "Resultado de E4a" y "Decisiones de E4b"), `docs/native/E4a-offers.md` y `docs/native/E4b-carte.md`. Los traspasos anteriores (`2026-09-22-hito6-e1-e2.md`, `2026-09-23-hito6-e3.md`) siguen valiendo para E1–E3 y las trampas del entorno.

## Decisiones del promotor (23-09-2026)

1. Orden aprobado ("Adelante con el plan"): **E3 → E4 → E8 reservas → E6 stock/compras/proveedores → E7 sincronización web → E5 usuarios → Hito 7 caja y TPV táctil → Hito 8 fiscal → informes**. Decisiones de cada corte delegadas al agente con todo documentado (plan antes de codificar, doc del corte con guion manual y capturas, AGENTS y STATUS).
2. Los merges los confirma él: "Sigue" y "ok, espero entonces" (esperando el instalador) valieron como confirmación de #62–#65; para la #66 hay que pedirla.
3. **Está probando el instalador en su PC**: instaló `Costina-Setup-0.29.0-e4.exe` (servicios Windows `Costina` y `CostinaPostgres`, `C:\Program Files\Costina`, puerto 5088). Se le pasó el enlace del `0.30.0-e5` (run 35834409407, artefacto `costina-windows-installer`). No tocar esa instalación.

## Estado verificable

- `develop` = `43fb9fb`: **Merge PR #62** (E3), **#63** (docs), **#64** (E4a, `8086aef`) y **#65** (traspaso). Instaladores de develop leídos: `0.29.0-e4` (7/7) y `0.30.0-e5` (7/7; D0 78/78).
- **PR #66 (E4b) abierta** contra `develop`: rama `feat/e4b-carta`, punta `ef86777`, 5 commits (plan; núcleo y esquema v6; servidor, `add-dish`, carta de ensayo, puerto de pruebas configurable; clientes WPF/PWA; docs y versión `0.31.0-e6`). Verificación local completa: dominio **79**, ClientChecks 75, checks del WPF con capturas (`docs/native/images/e4b/`), **14 suites HTTP** (`offers_http` 6 historias; `packaging_http.test_06e` hasta **v6**), Realtime 10, vitest 60, build de la PWA y **PWA real contra el motor** (plato de la carta pedido desde el comandero: 2 × croquetas, comensal 1, estación fría, cargo 16,00 €). **CI leída y verde sobre `ef86777`**: D0 79/79 en ambos sistemas; D1 con dominio 79, PersistenceChecks 40, las 14 suites HTTP (`offers` 6, `packaging` 12 hasta v6) y Playwright 5/5; desktop con vitest 60, ClientChecks 75 y WPF; servicio Windows 11/11. Con confirmación del promotor, mezclar: `Merge PR #66: E4b carta libre - items del catalogo por grupo y platos pedidos con add-dish`. Después leer el instalador de develop (`0.31.0-e6`, 7/7) y pasarle el enlace al promotor.
- Issues #26/#27/#28 abiertas hasta los guiones físicos; #25 no tocar.

## Qué trajo E4 (resumen)

**E4a** (`docs/native/E4a-offers.md`): esquema v5, `core.offers`/`offer_courses`/`offer_dishes`; un menú es un producto del catálogo (`person`) a tarifas E3; `Unit.AvailableOffer`; `offerId` con `menuId` de alias; `choose` en menús cerrados (`choice_missing`); vista ERP › Oferta; `configuration.offers`. **E4b** (`docs/native/E4b-carte.md`): esquema v6, `core.offer_items` y `core.products.station_id`; carta sin producto-menú con grupos `Optional` (`course_empty`); `add-dish` crea la elaboración por estación y apunta el cargo a la tarifa de la sala; ítems en ERP › Oferta, estación en ERP › Catálogo, "Pedir plato" en Comedor y PWA. Los productos-menú (categoría `menus`) no salen en los catálogos operativos.

## Trampas nuevas

- **Puerto 5088 ocupado** en el PC de desarrollo por la instalación real (servicio Windows, no se puede matar sin admin y es la prueba del promotor). Las suites locales corren en **5090** (`COSTINA_TEST_PORT`, exportado por `tools/agent/local-tests.sh`; `native_http`, `packaging_http` y `pwa_http` lo leen); el motor de laboratorio del navegador integrado también va en 5090. En CI sigue 5088.
- Al lanzar la batería en segundo plano con un subshell suelto, esperar la línea `exit` del log (`until grep -q '^exit' …`), no la notificación del agente.
- `psql` en Windows no decodifica acentos en la salida (`Ración` → `RaciÃ³n`): comparar códigos, no nombres, en las historias de empaquetado.
- Cambiar las fixtures de la demo obliga a repasar `catalog_http`, `offers_http`, `pwa_http` y `packaging_http` (listas exactas de vendibles, categorías y recuentos del manifiesto) y el E2E de Playwright (ya elige la oferta al abrir porque hay varias).
- Python de Windows no abre rutas `/c/...` de Git Bash: pasar rutas con `cygpath -w` o valores directos.

## Siguiente trabajo, en orden

1. Leer la CI de la **#66**, pedir el merge, leer el instalador de develop `0.31.0-e6` y pasar el enlace.
2. Decidir con el promotor si sigue **E4c** (combinados y modificadores: opciones con suplemento sobre un producto, elegibles en `add-dish`/`add-consumption`; y retirar los alias `menus`/`menuId`) o pasar ya a **E8 reservas** (libro con calendario, restaurante y villas, online y telefónicas). Propuesta del agente: E8 primero (es lo que el promotor citó como necesidad de Costiña) y E4c cuando llegue el TPV táctil.
3. Del promotor: fichero de exportación de Verial (importador), claves REST de WooCommerce (E7), guiones físicos, LICENSE.

## Prompt para continuar en otra sesión

```text
Soy Santi, promotor de Costiña / Hospitality OS (repo público Rivadesa/Costi-a, rama de integración `develop`; `main` NO se toca). Continúas el trabajo de otra sesión.
Lee primero, en este orden: docs/handoff/2026-09-23-hito6-e4.md (estado, decisiones, trampas y siguiente trabajo), dotnet/AGENTS.md (párrafos E1a a E4b), docs/STATUS.md, docs/plans/2026-09-23-e4-oferta-configurable.md y docs/plans/2026-09-23-referencia-verial.md. Las trampas del entorno están en los traspasos anteriores de docs/handoff/.
Reglas: un corte por PR desde `develop`; me pides confirmación para push+PR y, aparte, para cada merge (merge commit "Merge PR #N: ..." hecho en local y push); antes de cada push que toque dotnet/ corre `sh tools/agent/local-tests.sh` entero (en este PC en el puerto 5090: hay una instalación real en 5088 que no se toca) y para la PWA `npm test`, `npm run build` y comprobación visual; los tests de CI se LEEN en los logs (nunca fiarse del verde); GitHub sin gh CLI (tools/agent/gh.sh, cuerpos JSON en fichero UTF-8, tools/agent/wait-ci.sh); ediciones con scripts + tools/agent/patch.py (los heredocs largos de Bash fallan en Windows); cada corte trae doc en docs/native/, párrafo en dotnet/AGENTS.md, docs/STATUS.md, versión en csproj Server/Desktop, MainWindow.xaml y frontend/pwa/package.json; commits convencionales sin acentos con Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>; PR con resumen, decisiones, diferimientos, pruebas y esquema; nunca cierres #26/#27/#28 ni toques #25; pantallas cuidadas (Theme.xaml, estructura clásica de Windows con barra de menús) y captura antes de cerrar un corte; nunca metas credenciales en ninguna web.
Estado: develop = 43fb9fb (E1b, E2, E3 y E4a mezcladas). PR #66 (E4b carta libre, rama feat/e4b-carta, punta ef86777, versión 0.31.0-e6) abierta: lee su CI y pídeme el merge; después lee el instalador de develop 0.31.0-e6 y dame el enlace. Luego propón el plan del siguiente corte (E8 reservas, salvo que te diga E4c) como se hizo con E2, E3 y E4, y sigue el orden aprobado: E8 → E6 stock/compras → E7 web → E5 usuarios → Hito 7 caja → Hito 8 fiscal → informes.
```
