# Traspaso 2026-09-23 — Hito 6: E1b y E2 mezcladas, E3 (catálogo y tarifas) en PR

Para continuar desde cualquier equipo. Leer antes `dotnet/AGENTS.md` (párrafos E1a, E1b, E2, E3), `docs/STATUS.md`, `docs/plans/2026-09-23-e3-catalogo-y-tarifas.md`, `docs/native/E3-catalog.md` y `docs/plans/2026-09-23-referencia-verial.md`. El traspaso anterior (`2026-09-22-hito6-e1-e2.md`) sigue siendo válido para E1/E2 y para las trampas del entorno.

## Decisiones del promotor (23-09-2026)

1. **"Adelante con el plan"**: aprobado el orden **E3 catálogo → E4 oferta configurable → E8 reservas → E6 stock, compras y proveedores → E7 sincronización web → E5 usuarios → Hito 7 caja y TPV táctil → Hito 8 fiscal → informes**, el enfoque de E4 (oferta configurable: carta libre, menú del día o cerrado, degustación/experiencia, combinados, experiencia con alojamiento; **definición en el núcleo, ejecución en el módulo Dining**) y los tres merges pendientes (#59, #60, #61).
2. Las decisiones de cada corte siguen delegadas en el agente **con todo documentado** (plan antes de codificar, doc del corte con guion manual y capturas, AGENTS y STATUS).
3. Lo que el restaurante ya conoce (Verial) se toma como referencia de cobertura funcional y ergonomía, no de forma: ver `docs/plans/2026-09-23-referencia-verial.md`.

## Estado verificable

- `develop` = `91f537e`: **Merge PR #59** (E1b, `884682f`), **Merge PR #60** (E2, `98fe792`; su D0 se lanzó a mano tras el retarget: 71/71 en ambos sistemas) y **Merge PR #61** (docs). Instalador de `develop` leído: `Costina-Setup-0.28.0-e3.exe`, *Installer checks: 7 passed; 0 failed*.
- **PR #62 (E3) abierta** contra `develop`: rama `feat/e3-catalog`, punta `a7cf478`, 5 commits (plan; núcleo y esquema v4; servidor, CLI e importación; clientes WPF/PWA; docs y versión `0.29.0-e4`). Verificación local completa: dominio **76**, ClientChecks **75**, checks del WPF con capturas (`docs/native/images/e3/`), **13 suites HTTP** (nueva `catalog_http.py`; `packaging_http.test_06e` migra la base v1 real hasta **v4** y su copia v1 restaurada vende el catálogo migrado), Realtime **10**, vitest **60**, build de la PWA y **PWA real probada contra el motor** (dispositivo emparejado, consumo `wine/glass` cobrado a 9,50 € con producto, presentación y tarifa en la cuenta). **CI leída sobre `a7cf478`**: D0 76/76 en ambos sistemas; desktop (vitest 60, ClientChecks 75, WPF); D1 (dominio 76, PersistenceChecks 40, seguridad 6, las 13 suites HTTP con `packaging` 12 y `catalog` 5, Playwright 5/5) y `windows-service` 11/11. El primer intento de D1 falló en `station_http.test_06` (`KeyError: 'deviceToken'` al reemparejar un puesto, ruta que E3 no toca y que en local pasó tres veces); el reintento del job pasó entero: **fallo intermitente a vigilar** (si se repite, hacer que `pair_device` imprima la respuesta de `approve`/`collect`). El instalador no corre en la PR (no cambia empaquetado): leerlo en `develop` tras mezclar. Con confirmación del promotor, mezclar: `Merge PR #62: E3 catalogo y tarifas del nucleo, importacion CSV y pantalla ERP`.
- Issues #26/#27/#28 abiertas hasta los guiones físicos del promotor; #25 no tocar.

## Qué trajo E3 (resumen; el detalle en `docs/native/E3-catalog.md`)

Esquema **v4**: `core.taxes`, `core.categories` (jerárquicas, color), `core.products`, `core.presentations` (lo vendible), `core.tariffs` (`general` reservada), `core.prices` (vigencia, IVA incluido), `core.zones.tariff_id`; **`core.configuration` eliminada** (el núcleo ya no tiene configuración JSONB). Venta solo por `Unit.Sellable` + `Unit.PriceFor` (tarifa de la sala, general de respaldo, `price_missing`); el cargo congela producto, presentación y tarifa. Rutas `GET/POST /erp/catalog…` (main). `/catalog` y `/checkout/catalog` listan vendibles. CLI `import-catalog <csv>` (idempotente por huella, nunca borra) y `tools/catalog/woocommerce-to-csv.py`. Vista WPF **ERP › Catálogo y tarifas**; caja y PWA venden por presentación (`producto/presentación`).

## Trampas nuevas (además de las de los traspasos anteriores)

- Lanzar la batería HTTP en segundo plano con un subshell suelto: el agente recibe "completado" al instante; hay que leer el log hasta la línea `exit`. Una suite HTTP **no es repetible** sobre una base usada (códigos fijos): repetir siempre la batería entera. No compilar `Costina.Server` mientras corre.
- `test_06e` compara huellas entre la base migrada y la restaurada: cualquier escritura de prueba sobre la migrada tiene que hacerse **en la restaurada** (o después de comparar).
- `DateOnly` en Npgsql con `AddWithValue` funciona (columna `date`); `Domain.Catalog` colisiona con un método llamado `Catalog` en la misma clase estática (por eso `FullCatalog`).
- `printf` en Bash con rutas Windows (`\b`, `\n`) las rompe: escribir ficheros con Python o el Write tool.
- El navegador integrado: `find` a veces no encuentra nada aunque el DOM esté; usar `read_page` con `filter: interactive` y refs; la captura puede caducar si la ventana está detrás.

## Siguiente trabajo, en orden

1. Leer la CI de la **#62** y, con confirmación, mezclarla (instalador de `develop` `0.29.0-e4`).
2. **E4 — oferta configurable** (plan antes de codificar, versión `0.30.0-e5`): tipos de oferta en el núcleo (carta libre desde el catálogo, menú del día / cerrado con platos por pase y precio, degustación/experiencia como los menús actuales, combinados/modificadores, experiencia con alojamiento como referencia para E8), vigencia por fecha y servicio (comida/cena), y el módulo Dining ejecutando la oferta elegida al abrir la mesa (los menús fixture de `dining.configuration` migran a la nueva definición). Entrada de menú **ERP › Menús y pases** (renombrar a "Oferta: cartas y menús").
3. Después: E8 reservas (libro con calendario, restaurante y villas, online y telefónicas), E6 stock/compras/proveedores, E7 sincronización web (la tienda como tarifa; escritura por REST de WooCommerce con claves del promotor), E5 usuarios y módulos con pantalla.
4. Del promotor: fichero de exportación de Verial (fijar el formato del importador), claves REST de WooCommerce (E7), guiones físicos, LICENSE.

## Actualización de la tarde: E3 y E4a mezcladas

- `develop` = `8086aef`: **Merge PR #62** (E3, `112082d`), **Merge PR #63** (docs) y **Merge PR #64** (E4a, `8086aef`). Instalador de develop leído para E3: `Costina-Setup-0.29.0-e4.exe` 7/7; el de E4a (`0.30.0-e5`) es el que el promotor quiere probar ("ok, espero entonces"): leer su run y pasarle el enlace del artefacto `costina-windows-installer`.
- **E4a** (`docs/native/E4a-offers.md`, plan con sección "Resultado de E4a"): esquema **v5** (`core.offers`, `offer_courses`, `offer_dishes`; los menús JSONB del módulo migran a producto-menú + oferta `tasting`), un menú es un producto (`person`) a tarifas E3, `Unit.AvailableOffer`, `offerId` con `menuId` de alias, `CourseDefinition.ChoiceRequired` + `choose` + `choice_missing`, rutas `erp/offers`, `configuration.offers` (+ `menus` alias), vista ERP › Oferta, elecciones en Comedor y PWA; los productos-menú no salen en los catálogos operativos. CI leída sobre `2f60bfe` (D0 78, D1 14 suites + Playwright 5, desktop 75/WPF/vitest 60, servicio 11). El primer intento de CI falló en Playwright porque el E2E abría la mesa sin elegir oferta (antes solo había una y la PWA la preseleccionaba): corregido en el guion.
- Siguiente: **E4b** (carta libre `a-la-carte` con `add-dish`, `products.station_id`, catálogo por grupo en la PWA; combinados/modificadores), versión `0.31.0-e6`, plan en el mismo documento de E4. Después E8 reservas.

## Prompt para continuar en otra sesión

```text
Soy Santi, promotor de Costiña / Hospitality OS (repo público Rivadesa/Costi-a, rama de integración `develop`; `main` NO se toca). Continúas el trabajo de otra sesión.
Lee primero, en este orden: docs/handoff/2026-09-23-hito6-e3.md (estado, decisiones, trampas y siguiente trabajo), dotnet/AGENTS.md (párrafos E1a, E1b, E2 y E3), docs/STATUS.md, docs/plans/2026-09-23-e3-catalogo-y-tarifas.md y docs/plans/2026-09-23-referencia-verial.md. Las trampas del entorno están en docs/handoff/2026-09-22-hito6-e1-e2.md.
Reglas: un corte por PR desde `develop`; me pides confirmación para push+PR y, aparte, para cada merge (merge commit "Merge PR #N: ..." hecho en local y push); antes de cada push que toque dotnet/ corre `sh tools/agent/local-tests.sh` entero y para la PWA `npm test`, `npm run build` y comprobación visual; los tests de CI se LEEN en los logs (nunca fiarse del verde); GitHub sin gh CLI (tools/agent/gh.sh, cuerpos JSON en fichero UTF-8, tools/agent/wait-ci.sh); ediciones con scripts + tools/agent/patch.py (los heredocs largos de Bash fallan en Windows); cada corte trae doc en docs/native/, párrafo en dotnet/AGENTS.md, docs/STATUS.md, versión en csproj Server/Desktop, MainWindow.xaml y frontend/pwa/package.json; commits convencionales sin acentos con Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>; PR con resumen, decisiones, diferimientos, pruebas y esquema; nunca cierres #26/#27/#28 ni toques #25; pantallas cuidadas (Theme.xaml, estructura clásica de Windows con barra de menús) y captura antes de cerrar un corte; nunca metas credenciales en ninguna web.
Estado: develop = 8086aef (E1b, E2, E3 y E4a mezcladas; versión 0.30.0-e5). Sigue con E4b (carta libre, combinados y modificadores; ver la sección "Corte en dos PR" de docs/plans/2026-09-23-e4-oferta-configurable.md) proponiendo antes el plan como en E2/E3, y después el orden aprobado: E8 reservas → E6 stock/compras → E7 web → E5 usuarios → Hito 7 caja → Hito 8 fiscal → informes.
```
